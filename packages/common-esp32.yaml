packages:
  - !include debug-esp32.yaml
  - !include wifi-esp32.yaml

sensor:
  - platform: internal_temperature
    name: "Internal Temperature"
    disabled_by_default: true

wifi:
  min_auth_mode: WPA2
